<Window x:Class="TANGERINE_ZIP.ContextOperationWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Archive operation" WindowState="Normal" WindowStartupLocation="CenterScreen"
        Background="Black" Foreground="WhiteSmoke" FontFamily="Microsoft YaHei UI"
        Loaded="ContextOperationWindow_Shown" Closing="ContextOperationWindow_Closing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="_operationProgressBar" Grid.Column="0" Minimum="0" Maximum="100"
                         VerticalAlignment="Center" />
            <TextBlock x:Name="_progressPercentText" Grid.Column="1" Text="0%" Margin="4" />
        </Grid>
        <TextBlock x:Name="_engineText" Grid.Row="1" Margin="4" Visibility="Collapsed" />
        <TextBlock x:Name="_statusText" Grid.Row="2" Text="Operation in progress" Margin="4" />
        <Button x:Name="_actionButton" Grid.Row="3" Content="Stop" Click="ActionButton_Click" />
    </Grid>
</Window>
