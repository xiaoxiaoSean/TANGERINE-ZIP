<Window x:Class="TANGERINE_ZIP.ContextOperationWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Archive operation" WindowState="Normal" WindowStartupLocation="CenterScreen"
        Background="Black" Foreground="WhiteSmoke" FontFamily="Microsoft YaHei UI"
        Loaded="ContextOperationWindow_Shown" Closing="ContextOperationWindow_Closing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>
        <ProgressBar x:Name="_operationProgressBar" Grid.Row="0" Minimum="0" Maximum="100" />
        <TextBlock x:Name="_statusText" Grid.Row="1" Text="Operation in progress" Margin="4" />
        <Button x:Name="_actionButton" Grid.Row="2" Content="Stop" Click="ActionButton_Click" />
    </Grid>
</Window>
