<Window x:Class="TANGERINE_ZIP.OverwriteConflictWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File conflict" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="Black" Foreground="WhiteSmoke" FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="promptText" Grid.Row="0" Text="A file already exists at the destination" Margin="4" />
        <TextBox x:Name="detailText" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap" Margin="4" />
        <UniformGrid Grid.Row="2" Rows="1" Margin="0,12,0,0">
            <Button x:Name="thisYesButton" Content="Yes" />
            <Button x:Name="thisNoButton" Content="No" />
            <Button x:Name="allYesButton" Content="Yes to all" />
            <Button x:Name="allNoButton" Content="No to all" />
            <Button x:Name="cancelTaskButton" Content="Cancel task" />
        </UniformGrid>
    </Grid>
</Window>
