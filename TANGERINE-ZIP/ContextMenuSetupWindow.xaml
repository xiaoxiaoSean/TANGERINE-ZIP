<Window x:Class="TANGERINE_ZIP.ContextMenuSetupWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Context menu setup" WindowState="Normal" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="Black" Foreground="WhiteSmoke" FontFamily="Microsoft YaHei UI"
        Closing="ContextMenuSetupWindow_Closing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="descriptionText" Grid.Row="0" Text="Set up the Explorer context menu" Margin="4" />
        <StackPanel x:Name="menuModeOptions" Grid.Row="1" Margin="4">
            <RadioButton x:Name="groupedModeOption" GroupName="ContextMenuMode" IsChecked="True"
                         Foreground="{StaticResource ForegroundBrush}" Margin="4" />
            <RadioButton x:Name="directModeOption" GroupName="ContextMenuMode"
                         Foreground="{StaticResource ForegroundBrush}" Margin="4" />
        </StackPanel>
        <TextBlock x:Name="_statusText" Grid.Row="2" Margin="4" />
        <ProgressBar x:Name="_operationProgressBar" Grid.Row="3" Minimum="0" Maximum="100" />
        <ListBox x:Name="_progressLogList" Grid.Row="4" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="_startButton" Content="Start" Click="StartButton_Click" />
            <Button x:Name="_cancelButton" Content="Cancel" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
