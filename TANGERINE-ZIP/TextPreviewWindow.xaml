<Window x:Class="TANGERINE_ZIP.TextPreviewWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner" Background="Black" Foreground="WhiteSmoke"
        FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0">
            <Button x:Name="selectAllButton" Click="SelectAll_Click" />
            <Button x:Name="copyButton" Click="Copy_Click" />
            <Button x:Name="zoomInButton" Click="ZoomIn_Click" />
            <Button x:Name="zoomOutButton" Click="ZoomOut_Click" />
            <Button x:Name="saveAsButton" Click="SaveAs_Click" />
        </WrapPanel>
        <TextBox x:Name="previewText" Grid.Row="1" IsReadOnly="True"
                 TextWrapping="NoWrap" AcceptsReturn="True" AcceptsTab="True"
                 HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible"
                 SelectionBrush="Orange" />
    </Grid>
</Window>
